package android.app;

/**
 * This interface is used by the helper bridge library to call back into the
 * proxy manager.
 *
 * @hide
 */
public interface PermissionsPluginProxyCallbacks {
    /** This is intentionally left empty at the moment. */
}
